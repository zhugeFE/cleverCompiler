import * as React from 'react';
import './styles/jsonConfig.less'

interface Props {}
interface State {}
class GitJsonConfig extends React.Component<Props, State> {
  render () {
    return (
      <div className="git-json-config">json config</div>
    )
  }
}
export default GitJsonConfig